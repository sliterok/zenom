import { defineConfig } from "zenom"; // Assuming zenom exports defineConfig

export default defineConfig({
  // root: "./src", // Default is './'
  // output: "../dist", // Relative to root, default is './dist'
  backendConfig: {},
  frontendConfig: {},
  port: 3000,
});
